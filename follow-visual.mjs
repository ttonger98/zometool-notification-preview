const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function buildArt(remix){const points=remix?[[25,111],[82,80],[145,111],[82,143],[25,57],[82,25],[145,57],[82,89]]:[[20,123],[80,145],[150,110],[20,74],[80,96],[150,61],[83,25]];const edges=remix?[[0,1],[1,2],[2,3],[3,0],[4,5],[5,6],[6,7],[7,4],[0,4],[1,5],[2,6],[3,7],[4,1],[7,2]]:[[0,1],[1,2],[0,3],[1,4],[2,5],[3,4],[4,5],[3,6],[6,5],[4,6],[0,4],[1,5]];return `<svg viewBox="0 0 175 170" role="img" aria-label="${remix?'跟拼作品':'原造型'}拼搭示意"><ellipse cx="86" cy="151" rx="68" ry="10" fill="#34476815"/>${edges.map(([a,b],i)=>`<path d="M${points[a]} L${points[b]}" stroke="${['#4697df','#f1b748','#ed725e'][i%3]}" stroke-width="5" fill="none" stroke-linecap="round"/>`).join('')}${points.map(([x,y])=>`<circle cx="${x}" cy="${y}" r="5" fill="#faf6e6" stroke="#c0a76c" stroke-width="1.5"/>`).join('')}</svg>`;}
export function firstFollowVisual(m,event){return `<div class="follow-visual"><div class="follow-original">${buildArt(false)}<span>你的造型</span><strong>${escape(m.object.name)}</strong></div><div class="follow-arrow" aria-hidden="true">→</div><div class="follow-result">${buildArt(true)}<span class="follow-author"><span class="follow-avatar" aria-label="${escape(event.actor.name)}的头像">${escape(event.actor.name.slice(0,1))}</span>${escape(event.actor.name)}</span><strong>${escape(event.work?.name||'跟拼作品')}</strong></div></div>`;}
export function firstFollowPopup(m,event){return `<div class="overlay"><section class="dialog first-follow-dialog" role="dialog" aria-modal="true" aria-labelledby="popup-title"><button class="close" data-action="close-popup" aria-label="关闭首页弹窗">×</button><div class="source">来自：造型库</div><h2 id="popup-title">你的创意，有人跟着拼啦！</h2><p class="body-copy">${escape(event.actor.name)}跟着你的「${escape(m.object.name)}」拼出了新作品。</p>${firstFollowVisual(m,event)}<div class="dialog-actions"><button class="btn gold" data-action="popup-target">看看跟拼作品</button></div></section></div>`;}
